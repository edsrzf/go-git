package main

import (
	"fmt"
	"github.com/edsrzf/gogit"
)

func main() {
	//bytes, _ := ioutil.ReadFile("main.go")
	//b1 := NewBlob(bytes)

	/*bytes, _ := ioutil.ReadFile(".gitignore")
	b2 := NewBlob(bytes)

	t := NewTree(2)
	t.Add(".gitignore", b2)
	//t.Add("main.go", b1)

	p := []*Id{IdFromString("a67e49e02cb9a85ed457829c163c1162e15fcdc2")}
	c := NewCommit("Evan Shaw", "chickencha@gmail.com", &time{1277564665, -500}, "Evan Shaw", "chickencha@gmail.com", &time{1277568360, -500}, t.Id(), p, "Added some stuff\n")
	fmt.Println(c.Id().String())
	fmt.Println(string(c.Raw()))*/
}
